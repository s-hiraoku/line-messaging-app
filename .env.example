## Database (PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/line_messaging_app?schema=public

## NextAuth.js (Auth.js)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

## Admin (Credentials Provider)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

## Messaging API credentials are configured via the app UI (not env)
# Optional: Enable LINE login for NextAuth by providing these (not required for messaging)
# LINE_CHANNEL_ID=
# LINE_CHANNEL_SECRET=
# Note: Channel Access Token is stored in DB via Settings UI

## Redis (Upstash)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

## File Storage (Cloudinary)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

## Monitoring
SENTRY_DSN=
SENTRY_AUTH_TOKEN=
